@model EventDetailsViewModel
@{ }

<!-- EVENT TITLE -->
<div class="jumbotron jumbotron-fluid bg-rsi-red p-3 mb-3 rounded">
    <div class="container">
        <h1 class="display-6 text-center text-white">Are you sure you want to delete this event?</h1>
    </div>
</div>


<div class="container mt-5 mb-5 p-5 rsi-form">
    <form asp-action="Settings" method="post">
        <!-- EVENT NAME -->
        <div class="row">
            <label for="eventDeleteName" class="col-sm-2 col-form-label fw-bold">Name:</label>
            <div class="col-sm-7">
                <input type="text" class="form-control-plaintext" id="eventDeleteName" value="@Model.Event.Name">
            </div>
        </div>

        <!-- START -->
        <div class="row">
            <label for="eventDeleteStart" class="col-sm-2 col-form-label fw-bold">Start:</label>
            <div class="col-sm-7">
                <input type="text" class="form-control-plaintext" id="eventDeleteStart" value="@Model.Event.StartDate">
            </div>
        </div>

        <!-- END -->
        <div class="row">
            <label for="eventDeleteEnd" class="col-sm-2 col-form-label fw-bold">End:</label>
            <div class="col-sm-7">
                <input type="text" class="form-control-plaintext" id="eventDeleteEnd" value="@Model.Event.EndDate">
            </div>
        </div>

        <!-- DESCRIPTION -->
        <div class="row">
            <label for="eventDeleteDescription" class="col-sm-2 col-form-label fw-bold">Description:</label>
            <div class="col-sm-7">
                <textarea class="form-control-plaintext" id="eventDeleteDescription"
                          readonly>@Model.Event.Description</textarea>
            </div>
        </div>

        <!-- LOCATION -->
        <div class="row">
            <label for="eventDeleteLocation" class="col-sm-2 col-form-label fw-bold">Location:</label>
            <div class="col-sm-7">
                <input type="text" class="form-control-plaintext" id="eventDeleteLocation" value="@Model.Event.Location" readonly>
            </div>
        </div>

        <!-- BRANCH -->
        <div class="row">
            <label for="eventDeleteBranch" class="col-sm-2 col-form-label fw-bold">Branch:</label>
            <div class="col-sm-7">
                <input type="text" readonly class="form-control-plaintext" id="eventDeleteBranch" value="@Model.Event.Branch">
            </div>
        </div>

        <!-- TYPE -->
        <div class="row">
            <label for="eventDeleteType" class="col-sm-2 col-form-label fw-bold">Event Type:</label>
            <div class="col-sm-7">
                <input type="text" readonly class="form-control-plaintext" id="eventDeleteType" value="@Model.Event.Type">
            </div>
        </div>

        <!-- ATTACHMENTS -->
        <div class="row top-50">
            <label for="eventDeleteAttachments" class="col-sm-2 col-form-label fw-bold">Attachments:</label>
            <div class="col-sm-7">
                <ul class="list-group" id="eventDeleteAttachments">
                    @foreach (var attachment in Model.Attachments)
                    {
        <li class="list-group-item">
            <a href="@attachment.Link"
               class="link-primary">
                @attachment.Title
            </a>
        </li>
}
                </ul>
            </div>
        </div>

        <!-- BUTTONS -->

    <div class="form-group row mt-5">
        <div class="col-sm-2"></div>
        <div class="col-sm-7">
            <div class="btn-group-lg">
                <!-- EDIT -->
                <a class="btn rsi-btn-blue shadow-sm"
                   asp-area="CulAm"
                   asp-controller="CulAmEvent"
                   asp-action="Edit"
                   asp-route-id="@Model.Event.EventID">Edit</a>
                <!-- DELETE -->
                <a class="btn rsi-btn-red shadow-sm"
                   asp-area="CulAm"
                   asp-controller="CulAmEvent"
                   asp-action="Delete"
                   asp-route-id="@Model.Event.EventID">Delete</a>
            </div>
        </div>
    </div>

        <!-- TODO: This should be radio buttons because a user shouldnt be able to check yes AND no.-->
        <!-- CALL TO ACTION -->
        <!--<div class="row mt-5">
    <label class="col-sm-2"></label>
    <div class="col-sm-7">
        <fieldset class=" p-3 border border-dark border-1 shadow-sm rounded">
            <legend class="fw-bold">Are you coming?</legend>-->
        <!-- YES -->
        <!--<div class="form-check">
    <label class="form-check-label" for="eventDetailsComingYes">Yes</label>
    <input class="form-check-input" type="checkbox" value="" id="eventDetailsComingYes">-->
        <!-- TODO: this should be checked or unchecked dynamically -->
        <!--</div>-->
        <!-- NO -->
        <!--<div class="form-check">
    <label class="form-check-label" for="eventDetailsComingNo">No</label>
    <input class="form-check-input" type="checkbox" value="" id="eventDetailsComingNo">-->
        <!-- TODO: this should be checked or unchecked dynamically -->
        <!--</div>
            </fieldset>
        </div>
    </div>-->
    </form>
</div>
